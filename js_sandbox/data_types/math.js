const num1 = 100;
const num2 = 50;
let val;

// Simple math with numbers

val = num1 + num2;
val = num1 * num2;
val = num1 - num2;
val = num1 / num2;
val = num1 % num2;

// Math Object
val = Math.PI;
val = Math.E;
val = Math.round(2.4);  // automatic rounding
val = Math.ceil(2.01);  // rounds to next whole int
val = Math.floor(2.9);  // rounds to previous whole int
val = Math.sqrt(64);
val = Math.abs(-3);
val = Math.pow(2, 3);
val = Math.min(1, 2, 3, 4, 5, 6, 7);
val = Math.max(1, 2, 3, 4, 5, 6, 7);
val = Math.random();

console.log(val);